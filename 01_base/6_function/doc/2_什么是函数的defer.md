# 1.概念
等同于java中的finally关键字。即连接数据库、打开文件、开始锁等这些操作，无论如何最后都要记得去释放资源。<br/>
# 2.defer的作用
defer的作用：defer后面的代码会放在函数return之前执行。<br/>
一个语句中可以有多个defer，其先后顺序为：先进后出。
相当于把finally中的代码放到了申请资源一起，这个是因为go语言做的优化：认为申请和释放 不应离得太远，以免释放遗漏，所以提供了defer。